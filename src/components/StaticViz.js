import React from "react";
import StaticCarContainer from "../containers/StaticCarContainer";

let StaticViz = (props) => {
  return (
    <React.Fragment>
      <StaticCarContainer />
    </React.Fragment>
  );
};
export default StaticViz;
